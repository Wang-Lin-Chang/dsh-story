# dsh-story 实验记录

> 原则：没实测不声称。全部实验装置（foreshadow-demo / story-invariant-demo / demo2 / perf / feisheng / real-book）的判决按原始数据核对记录于本文。

## EXP-1 伏笔债务审计（foreshadow-demo.mjs）

120 章模拟书 × 12 伏笔 × 6 项债务注入（2 超期 + 3 应收未收 + 1 幽灵回收）→ **100% 召回 + 零误报**，引擎 0.10ms。首轮"9 报 vs 注入 6"暴露注入不正交（双债同时成立）——装置变量控制修正后全绿。对照组：30 万字人工通读 ≈ 10 小时。

## EXP-2 叙事不变量引擎 v0（story-invariant-demo.mjs）

300 章 × 15 人物 × 428 事件 × 8 类规则注入 → **8/8 召回 + 零误杀**。审计实验连环抓三处装置/规则缺陷（等级链参数缺失 / 资产查投影表而非回放 / 初始境界在快照表）→ 定稿真理模型：**初始快照 + 事件流回放**。

## EXP-3 规则库扩展（story-invariant-demo2.mjs）

新增 6 类（幽灵地点/年龄单调/神器唯一/债务余额/同名冲突/门派忠诚）→ **6/6 召回 + 零误杀**（含合法叛门对照组不误报）。规则库达 14 类。

## EXP-4 大规模性能（story-invariant-perf.mjs）

1000 章 × 100 人物 × 20 万事件流。性能优化链：210ms →（SQL JOIN 聚合 2979ms 劣化弃用）→ 索引+JS 回放 167ms → Set 去重 145ms → **"初始快照事件化" + 纯 SQL 聚合 = 83.7ms**（<100ms 达标）。最有价值判决：建档即写 chapter=0 种子事件——真理模型统一为单一事件流，表降级为投影（进 story-spec 草案第 1 节）。

## EXP-5 《飞升之后》真实设定（story-invariant-feisheng.mjs）

真实设定（楚渊/苏云栖/壁垒/修真历）300 章模拟书 × 5 类注入 → **5/5 召回 + 零误杀**。

## EXP-6 真实文章实战（real-book.mjs）

《飞升之后》ch001/ch002 真实文本（5655 字）全流程：模板开书 → 写前状态卡 → 意图锚点 → 真实文本入章 → 结算 OK → 字数窗提示（ch002 超 7 字）→ 伏笔热度跟踪 → 漂移注入（50 万灵石）当场枪毙 → 全审计 0.44ms。

## EXP-7 插件化（test/plugin-integration.test.ts）

五工具（story_new/draft/settle/audit/world）+ 多书管理器 → **9/9 断言**。集成测试抓出模块合并的功能退化：settle 丢了原型的即时守恒校验（"口袋没这么多钱"当场拒入账）——已修复回 settle。

